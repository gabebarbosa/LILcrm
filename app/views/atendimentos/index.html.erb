<p id="notice"><%= notice %></p>

<h1>Atendimentos</h1>

<table class="table table-bordered">
  <thead>
    <tr>
      <th>Código</th>
      <th>Assunto</th>
      <th>Descricao</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @atendimentos.each do |atendimento| %>
      <tr>
        <td><%= atendimento.id %></td>
        <td><%= atendimento.assunto %></td>
        <td><%= atendimento.descricao %></td>
        <td><%= atendimento.status %></td>
        <td><%= link_to 'Ver', atendimento %></td>
        <td><%= link_to 'Editar', edit_atendimento_path(atendimento) %></td>
        <td><%= link_to 'Deletar', atendimento, method: :delete, data: { confirm: 'tem certeza que deseja deletar o atendimento?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo Atendimento', new_atendimento_path %>
