<%= form_for(@comentario) do |f| %>
  <% if @comentario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comentario.errors.count, "error") %> prohibited this comentario from being saved:</h2>

      <ul>
      <% @comentario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 
<div class="panel panel-default">
  <div class="panel-body">
      <%= f.hidden_field :user,:value => current_user.email, class: "form-control" %>
    <div class="form-group">
      <%= f.label "Novo Comentário" %><br>
      <%= f.text_area :descricao, class: "form-control", rows: 4 %>
    </div>
    <%= f.submit "Adicionar comentario", class: "btn btn-default btn-xs" %>
      <% f.label :atendimento_id %><br>
      <% f.number_field :atendimento_id %>
    <%= f.hidden_field :atendimento_id %>
    
   
  <% end %>
  </div>
</div>